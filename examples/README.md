This directory contains the Pyramis specification files for:
- Login Server with a timeout.
- 5G Access and Mobility Function.

`Pyramis build <NFName>` generates a multithreaded C++ server implementation from the specification files. 
- `login-server-ref` contains a generated C++ server for reference.

